# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 仓库用途

这是 GitHub 个人主页展示仓库（`cv-r/cv-r`），唯一的文件 `README.md` 会渲染为 GitHub 个人 Profile 页面。

## 修改 README 注意事项

- **这是公开页面**，禁止包含手机号、详细日期、年龄、薪资等个人隐私
- **双语结构**：文档包含中英文两版，英文在上（默认），中文在下以 `## 中文版本` 分隔
- 修改内容时需同步更新两个语言的对应部分，保持信息一致
- 文件路径即 `README.md`，无其他源码
- 风格简洁，定位为 AI 前端工程师

## 常用命令

```bash
git add README.md && git commit -m "docs: <描述>" && git push
```
